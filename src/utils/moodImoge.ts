export const moodImoge = (mood: string) => {
    if (mood === "보통") { return "😑" }
    if (mood === "뿌듯") { return "😊" }
    if (mood === "행복") { return "😄" }
    if (mood === "설렘") { return "🥰" }
    if (mood === "평온") { return "😌" }
    if (mood === "슬픔") { return "😭" }
    if (mood === "피곤") { return "😩" }
    if (mood === "불안") { return "😰" }
    if (mood === "우울") { return "😔" }
    if (mood === "화남") { return "😡" }
}